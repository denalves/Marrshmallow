import styled from "styled-components";
import { Container } from "../../globalStyles";

export const ContainerPlans = styled(Container)`
  padding-top: 60px;
  ${Container};
`;

export const CardsPlan = styled.div`
  display: flex;
  justify-content: space-between;
  flex-wrap: wrap;
 justify-content: center;
`;
